{
  "name": "gpx-collaboration",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@8.15.5",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "concurrently \"pnpm --filter web dev\" \"pnpm --filter server dev\"",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
